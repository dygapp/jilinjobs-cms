# Current Work

当前没有 Ready / active Execution Unit，因此本目录只保留该说明。

新的 work artifact 只有在上游 Ready、`slice-work` 已形成 Candidate Execution Unit 且 `readiness-check` PASS 后，才可按 Consumer-local Method 进入这里并取得 Execute Authority。完成 Integration 与 Post-Integration Current Evidence 后应移入 `../archive/`。
