# Git Commit 规范（过渡 Locator）

> **TRANSITIONAL LOCATOR — Issue #153 Foundation Rebuild**
>
> 本文件不再拥有 Commit Message、Type / Scope、history rewrite 或 merge boundary 的规范正文。它只兼容尚未迁移的旧引用，Foundation locator 迁移完成后删除。

当前 canonical owner：

- `docs/rules/repository/git-commit-governance.md`（`rule:git-commit-governance`）

与 Commit 重写相关的 Evidence currentness 继续由当前 Verification Rules 与 Method / Repository Authority 决定；本文件不授予 merge / release / deploy 权限。