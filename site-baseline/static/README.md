# 初始化静态资源包

本目录是中心主站版本化的初始化静态资源包。运行环境启动时应将本目录内容作为 `CMS_STATIC_ROOT` 的初始文件集，而不是由 E2E 测试动态创建站点基础资源。

当前仅固化已经确认且不依赖历史内容迁移的技术基线资源。原站 Logo、Banner、师资头像等实际运营素材在完成来源确认与人工视觉复核后按同一目录机制补充；不得为了满足初始化流程而伪造产品素材。

`health/baseline.png` 是用于证明初始化静态资源包已被运行环境实际挂载的无业务含义标记文件，不作为前台展示素材。
